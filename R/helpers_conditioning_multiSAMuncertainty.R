multiSAMuncertainty <- function(SAMfit) {
  
  # ------------------------#
  # Definitions
  # ------------------------#
  
  ### catch fleet index/indices
  catch_fleets <- which(SAMfit$data$fleetTypes == 0)
  catch_desc <- SAMfit$data$aux
  years      <- SAMfit$data$years
  
  # -----------------------------#
  # Catches - Standard deviation 
  # -----------------------------#
  
  ## Define index
  
  ## FLQuant structure
  
  variates[,colnames(variates) == "logSdLogObs"]
    
  # -----------------------------#
  # Surveys - Standard deviation
  # -----------------------------#
}